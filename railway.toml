[build]
builder = "RAILPACK"
rootDirectory = "."
watchPatterns = ["apps/server/**", "package.json"]

[deploy]
startCommand = "node apps/server/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
