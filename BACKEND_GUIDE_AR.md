# دليل الخادم المركزي (Backend) — شرح مبسط

هذا الدليل يشرح الجزء الجديد الذي أضيف إلى مستودع المشروع: **الخادم المركزي** الذي ينقل النظام من العمل على جهاز واحد إلى العمل المركزي مع قاعدة بيانات موحدة.

## ماذا أضفنا؟

أنشأنا مجلدًا جديدًا داخل المستودع هو `apps/server` يحتوي على برنامج الخادم كاملًا. هذا البرنامج يعمل على Railway (أو أي استضافة Node.js) ويرتبط بقاعدة بيانات PostgreSQL مركزية.

| المكوّن | الوظيفة |
|---|---|
| `sql/schema.sql` | مخطط قاعدة البيانات: أكثر من 30 جدولًا تغطي كل عمليات ERP |
| `sql/seed.sql` | تهيئة أولية: الأدوار والصلاحيات ودليل الحسابات المحاسبي والمستخدم الإداري |
| `server.js` | نقطة تشغيل الخادم (Express) |
| `engine/accounting.js` | محرك المحاسبة المزدوجة: ينشئ القيود تلقائيًا ويقفل الفترات ويحسب التقارير |
| `middleware/auth.js` | المصادقة والصلاحيات: لا يمكن تنفيذ أي عملية بدون تسجيل دخول، ولا تنفيذ أي إجراء بدون صلاحية |
| `middleware/audit.js` | سجل التدقيق: كل عملية تُسجل بمنفذها وتاريخها |
| `routes/` | 13 ملفًا لنقاط API تغطي كل الوحدات |

## الجداول الرئيسية في قاعدة البيانات

المحاسبة: دليل الحسابات (`chart_of_accounts`)، قيود اليومية (`journal_entries` و`journal_lines`)، إقفال الفترات (`period_closes`).
العمليات: الأصناف والدفعات (`items`, `batches`, `stock_movements`)، المشتريات (`purchases`, `purchase_lines`)، فواتير المبيعات والمرتجعات (`sales_invoices`, `sales_returns`)، التحصيل (`collections`)، سداد الموردين (`supplier_payments`)، التحويلات (`transfers`).
الكيانات: العملاء والموردون والأطباء.
الخزينة: الصناديق (`cash_boxes`)، الحسابات البنكية (`bank_accounts`)، سندات الصرف (`receipts`).
الإدارة: المستخدمون والأدوار والصلاحيات (`users`, `roles`, `role_permissions`)، الفروع والمخازن (`branches`, `stores`)، سجل التدقيق (`audit_logs`).

## خطوات التشغيل على Railway (مجاني)

**الخطوة الأولى — قاعدة البيانات:**
1. افتح https://railway.app وسجّل الدخول بحساب GitHub.
2. اضغط **New Project** ثم **New** واختر **PostgreSQL**.
3. ستُنشأ قاعدة بيانات تلقائيًا.

**الخطوة الثانية — الخادم:**
1. في نفس المشروع اضغط **New** واختر **Deploy from GitHub repo**.
2. اختر مستودع `shrfahmdbdalrb49-gif/Eeerp`.
3. افتح إعدادات الخدمة ← **Variables** وأضف:
   - `PORT` بقيمة `4000`
   - `JWT_SECRET` بقيمة عشوائية طويلة (اكتب `openssl rand -base64 32` في أي طرفية أو استخدم أي سلسلة عشوائية من 32 حرفًا على الأقل).
   - Railway يضيف تلقائيًا متغير `DATABASE_URL` الخاص بقاعدة PostgreSQL.
4. سيبدأ النشر تلقائيًا. بعد دقائق، افتح الرابط الذي أعطاه لك Railway (مثل `sharaf-server.up.railway.app`) وألحق به `/health` للتأكد أن الخادم يعمل.

**الخطوة الثالثة — تهيئة الجداول:**
بعد أول تشغيل، أنشئ الجداول من وحدة Railway Console:
1. في لوحة المشروع اضغط **Open Console** على خدمة PostgreSQL.
2. الصق محتوى `apps/server/sql/schema.sql` واضغط تشغيل.
3. ثم الصق محتوى `apps/server/sql/seed.sql` واضغط تشغيل.

## الحساب الإداري الافتراضي

بعد التهيئة يوجد حساب: `admin` / `Admin@1234` (الدور: مدير). **غيّر كلمة المرور فور أول دخول.**

## الأدوار والصلاحيات

| الدور | الصلاحيات الأساسية |
|---|---|
| admin | كل شيء دون استثناء |
| accountant | القيود المحاسبية، دليل الحسابات، التقارير، التدقيق + قراءة البيع والشراء |
| sales | إنشاء الفواتير والتحصيل + قراءة الأصناف والعملاء |
| cashier | البيع والتحصيل + قراءة الأصناف |
| warehouse | المشتريات والمخزون + قراءة الأصناف |
| viewer | قراءة فقط |

## ماذا بعد؟

هذا الخادم أصبح جاهزًا في المستودع، وربطه بالواجهة (الويب الحالي) هو الخطوة القادمة: تعديل الواجهة لتقرأ البيانات من API المركزي بدل IndexedDB المحلي، مع الحفاظ على دعم العمل دون اتصال. هذه الخطوة تتم في جلسة مستقلة بعد أن تشغّل الخادم على Railway وترسل لي الرابط.
